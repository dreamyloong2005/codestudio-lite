# Progress

## 2026-06-23

- Started comparison of codestudio-lite forced Codex plugin unlock against local Codex++ source.
- Confirmed `D:\迅雷下载\CodexPlusPlus-main` exists.
- Located codestudio-lite implementation in `src-tauri/src/core/codex_client.rs`.
- Located Codex++ implementation in `assets/inject/renderer-inject.js`, `crates/codex-plus-core/src/launcher.rs`, `crates/codex-plus-core/src/bridge.rs`, and `crates/codex-plus-core/src/assets.rs`.
- Determined codestudio-lite shares the CDP/runtime DOM-unlock idea, but lacks Codex++'s modern marketplace request/filter unlock layer.
- Started implementation follow-up to add Codex++-style modern marketplace unlock behavior behind the existing `patchForcePluginUnlock` setting.
- Added a failing unit test for modern marketplace unlock signals, verified it failed, then implemented the missing layer in `src-tauri/src/core/codex_client.rs`.
- Added `Page.addScriptToEvaluateOnNewDocument` registration before the existing immediate `Runtime.evaluate`.
- Verification passed: targeted unit test, `cargo test codex_client --manifest-path src-tauri/Cargo.toml`, `npm run test:unit`, `npm run check`, and `npm test`.
- Full `cargo test --manifest-path src-tauri/Cargo.toml` still has an unrelated stable failure in `core::profile::tests::codex_official_config_uses_openai_auth_without_token`; the failing file was not changed by this task.
- Diagnosed Windows Codex update SHA-256 failure: live checksums now contain both arm64 and x64 MSIX entries, with arm64 first, while `/latest/win` downloads x64. Added a focused failing regression test for the checksum selection bug.
- Verified the regression test fails on the old logic, returning the arm64 hash instead of the x64 hash. Implemented a narrow checksum fix that matches the Windows package moniker exactly and only falls back to `.Msix` when the checksum file has a single MSIX entry.
- Verification after the fix: `cargo test --manifest-path src-tauri/Cargo.toml windows_checksum_matches_package_moniker_not_first_msix -- --nocapture` passed, and `cargo test --manifest-path src-tauri/Cargo.toml codex_client -- --nocapture` passed. `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` reported only formatting in the new assertion, so formatting is being applied.
- Re-ran final verification after formatting: `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` passed, `cargo test --manifest-path src-tauri/Cargo.toml windows_checksum_matches_package_moniker_not_first_msix -- --nocapture` passed, and `cargo test --manifest-path src-tauri/Cargo.toml codex_client -- --nocapture` passed. Marked Phase 6 and Phase 7 complete.
- Resumed warning cleanup. Reproduced the warning set as hard errors with `RUSTFLAGS=-D warnings cargo test --manifest-path src-tauri/Cargo.toml windows_checksum_matches_package_moniker_not_first_msix -- --nocapture`; the failures were dead-code errors for macOS-only accessibility constants/enum and debugger title helpers compiled on Windows. Added cfg gates for macOS-only implementations and test-visible title helpers.
- Continued warning cleanup after interruption: converted more macOS-only call sites from runtime `cfg!` to compile-time `#[cfg]`, gated macOS restart/accessibility helpers, and gated `profile::ensure_claude_desktop_developer_mode` to macOS/test so Windows lib builds no longer compile unused macOS-only symbols.
- Further tightened warning cleanup: scoped `std::io::Write` and macOS Accessibility restart marker helpers to macOS only, and removed test visibility from the runtime-only profile developer-mode entrypoint. The source-inspection tests still see the text through `include_str!`, but Windows test binaries no longer compile unused runtime helpers.
- `claude_desktop_patch::tests` initially failed after cfg cleanup because one source-inspection test split on the old exact `#[cfg]` + signature text. Updated the test to split by function name and the next function boundary instead.
- Adjusted the brittle macOS source-inspection test after cfg cleanup: it now inspects the actual `macos_accessibility_preflight` function body instead of assuming an exact attribute/signature string around the wrapper function.
- Final warning cleanup verification passed: `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`, `RUSTFLAGS=-D warnings cargo test --manifest-path src-tauri/Cargo.toml windows_checksum_matches_package_moniker_not_first_msix -- --nocapture`, `cargo test --manifest-path src-tauri/Cargo.toml codex_client -- --nocapture`, and `cargo test --manifest-path src-tauri/Cargo.toml claude_desktop_patch::tests -- --nocapture` all passed.
- Resumed after user confirmation that Windows has the same official Claude Desktop debugger route as macOS. Added Phase 8/9 to remove the old Windows in-place patch scheme and replace it with runtime-only debugger injection.
- Added a failing Rust regression test proving Windows localization must be runtime-only and must not retain old app.asar/fuse/elevated-patch symbols. Verified it failed on the old Windows `ensure_localization_patch() -> apply_localization_patch()` path.
- Began implementation: changed Windows localization preparation to `ensure_patch_files()` + Developer Mode, changed localized launch to start official Claude then call `ensure_windows_claude_main_process_debugger()` and `retry_inject_localization()`, removed old Windows install-patch implementation blocks, and updated JS source checks toward the runtime-only design.
- Completed the Windows runtime-only rewrite: removed old Windows app.asar/fuse/elevated-patch helpers, deleted the obsolete ASAR test helper, made macOS debugger code platform-gated, added Windows debugger request flow, removed duplicate direct-launch background injection, and updated stale comments/tests from "patching" to runtime localization.
- Final verification passed: `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`, `RUSTFLAGS=-D warnings cargo test --manifest-path src-tauri/Cargo.toml claude_desktop_patch::tests -- --nocapture`, `cargo test --manifest-path src-tauri/Cargo.toml claude_desktop_patch::tests -- --nocapture`, and `npm run test:unit -- src/lib/desktopClientPages.test.mjs`. A targeted `rg` sweep for old Windows patch symbols in the touched runtime/test files returned no matches.
- Investigated the follow-up "Windows cannot select Chinese" report. Added a failing regression assertion showing Windows direct localized launch did not prepare runtime files before writing the zh marker. Fixed `launch_windows_claude_desktop(localize)` to call `ensure_patch_files()?` and `ensure_claude_desktop_developer_mode()?` before `write_localized_launch_marker()?`, so the language whitelist/radio runtime can load.
- Verification for the Windows Chinese selection fix passed: `cargo test --manifest-path src-tauri/Cargo.toml windows_localization_is_runtime_only_and_does_not_patch_installed_app -- --nocapture`, `cargo test --manifest-path src-tauri/Cargo.toml claude_desktop_patch::tests -- --nocapture`, `npm run test:unit -- src/lib/desktopClientPages.test.mjs`, `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`, and `RUSTFLAGS=-D warnings cargo test --manifest-path src-tauri/Cargo.toml windows_localization_is_runtime_only_and_does_not_patch_installed_app -- --nocapture`.

## 2026-06-24

- Investigated Windows reports that direct Claude Desktop launch blocks CodeStudio Lite and uninstall fails with exit code `-1978335210`. Confirmed the launch regression came from synchronous debugger enablement/injection in `launch_windows_claude_desktop(localize)`, which can wait up to the debugger timeout inside the Tauri command.
- Added failing regression checks requiring direct localized Windows launch to spawn the silent localization injector in the background instead of synchronously calling `ensure_windows_claude_main_process_debugger()?` and `retry_inject_localization()?`. Fixed the launch path to prepare runtime files and the zh marker, launch official Claude, then return after starting the background injector.
- Decoded uninstall exit code `-1978335210` to `0x8A150016` and traced it to ambiguous winget package matching. Added Claude Desktop Windows uninstall routing that bypasses winget: MSIX uses direct AppX removal through detected package identities, and native EXE keeps the registry uninstall script.
- Added tests for the Claude-specific uninstall path and the `0x8A150016` explanation. Verification passed: `npm run test:unit -- src/lib/desktopClientPages.test.mjs`, `cargo test --manifest-path src-tauri/Cargo.toml tool_installer -- --nocapture`, `cargo test --manifest-path src-tauri/Cargo.toml claude_desktop_patch::tests -- --nocapture`, `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`, `RUSTFLAGS=-D warnings cargo test --manifest-path src-tauri/Cargo.toml tool_installer -- --nocapture`, and `RUSTFLAGS=-D warnings cargo test --manifest-path src-tauri/Cargo.toml direct_claude_desktop_launch_spawns_background_injector_on_windows -- --nocapture`.
- Resumed after the user reported "uninstall command finished, but verification still detects it." Local read-only checks found no registered Claude MSIX through `Get-AppxPackage`, but did find the native EXE install in HKCU uninstall registry (`AnthropicClaude`, version `1.14271.0`, `C:\Users\dreamyloong\AppData\Local\AnthropicClaude`). Root cause: uninstall verification still used global Claude Desktop detection, so removing one selected install kind could be marked failed when another kind remained.
- Added red/green regression tests for selected-kind verification and success messages. Fixed Windows Claude Desktop uninstall to resolve the effective selected kind before running the command, verify MSIX removal with registered AppX presence rather than stale AppX residue, verify EXE removal independently, and explain when the other kind or stale residue still exists. Verification passed: targeted new tests, `cargo test --manifest-path src-tauri/Cargo.toml tool_installer -- --nocapture`, `cargo test --manifest-path src-tauri/Cargo.toml package -- --nocapture`, `RUSTFLAGS=-D warnings cargo test --manifest-path src-tauri/Cargo.toml tool_installer -- --nocapture`, and `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`.
- Followed up after the user noted that the app could still resolve Claude Desktop to a `WindowsApps` directory. Root cause: the normal `detect_claude_desktop_windows()` and `claude_desktop_install_kinds()` paths still fell back to `appx-stale` package directories after registered MSIX lookup failed. Added a failing detector test for stale MSIX payloads not marking the tool installed, then removed stale MSIX from normal installed-state and per-kind detection while keeping stale manifest helpers for launch-time MSIX registration repair.
- Verification for the stale WindowsApps detection fix passed: `cargo test --manifest-path src-tauri/Cargo.toml detector -- --nocapture`, `npm run test:unit -- src/lib/desktopClientPages.test.mjs`, `cargo test --manifest-path src-tauri/Cargo.toml tool_installer -- --nocapture`, `cargo test --manifest-path src-tauri/Cargo.toml package -- --nocapture`, and `RUSTFLAGS=-D warnings cargo test --manifest-path src-tauri/Cargo.toml detector -- --nocapture`.
- Tightened Windows Claude Desktop uninstall to fully delete and verify selected install-kind files. Added red/green tests for MSIX payload cleanup and EXE install-root cleanup; implemented a Claude-specific WindowsApps cleanup report that verifies package identity, publisher suffix, manifest, and `app\Claude.exe` before deleting, uses an elevated fallback with repeated safety checks, and fails uninstall if payloads remain or WindowsApps cannot be confirmed. Updated the EXE uninstall script to remove registry `InstallLocation`/common Claude roots and fail if any root still exists.
- Verification for the full-delete uninstall change passed: `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`, `cargo test --manifest-path src-tauri/Cargo.toml package -- --nocapture`, `cargo test --manifest-path src-tauri/Cargo.toml tool_installer -- --nocapture`, `cargo test --manifest-path src-tauri/Cargo.toml detector -- --nocapture`, `RUSTFLAGS=-D warnings cargo test --manifest-path src-tauri/Cargo.toml tool_installer -- --nocapture`, `npm run test:unit -- src/lib/desktopClientPages.test.mjs`, and `git diff --check` (only LF/CRLF warnings, no whitespace errors).
- Rechecked the live machine after the interruption. The registered Claude MSIX and WindowsApps payload directory were gone, and no `cowork-svc` process was running, but `CoworkVMService` still existed as a stopped Automatic service because service deletion needs elevation. This explains why repeated non-elevated CodeStudio Lite uninstall attempts could remove package/files yet still leave Windows service registration residue.
- Added red/green coverage for the service lock/residue case. The MSIX cleanup script now treats safe partial Claude WindowsApps directories as removable when only `app`, `app\resources`, or `app\resources\cowork-svc.exe` remains. The Windows uninstall path now stops `CoworkVMService` / `cowork-svc` before file cleanup and removes/verifies the service after cleanup, including an elevated fallback for service deletion.
- Final verification passed after formatting the interrupted patch: `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`, `cargo test --manifest-path src-tauri/Cargo.toml claude_desktop_windows_uninstall_stops_cowork_service_before_file_cleanup -- --nocapture`, `cargo test --manifest-path src-tauri/Cargo.toml claude_msix_payload_cleanup_script_includes_partial_package_residue -- --nocapture`, `cargo test --manifest-path src-tauri/Cargo.toml package -- --nocapture`, `cargo test --manifest-path src-tauri/Cargo.toml tool_installer -- --nocapture`, `npm run test:unit -- src/lib/desktopClientPages.test.mjs`, `RUSTFLAGS=-D warnings cargo test --manifest-path src-tauri/Cargo.toml tool_installer -- --nocapture`, `cargo test --manifest-path src-tauri/Cargo.toml detector -- --nocapture`, and `git diff --check` (only LF/CRLF warnings, no whitespace errors).
- Started Phase 15 for Claude Desktop install/update progress. Initial research shows Codex Desktop uses structured `CodexClientProgress` with phase/message/downloaded/total/percent/step fields, while Claude currently only consumes generic `ToolInstallProgress` command logs.
- Completed Phase 15. Extended generic `ToolInstallProgress` with optional structured progress fields, taught Claude Desktop per-kind state to retain `progress` alongside console logs, added the Codex-style progress panel to `ClaudeDesktop.svelte`, and localized the new progress strings in en-US/zh-CN/zh-TW.
- Reworked the Claude Windows MSIX install/update path so Rust downloads `https://claude.ai/api/desktop/win32/x64/msix/latest/redirect` with streaming byte progress, then passes the downloaded local MSIX to `Add-AppxPackage -Path`; the PowerShell script no longer performs its own download.
- Verification for Phase 15 passed: `npm run test:unit -- src/lib/desktopClientPages.test.mjs`, `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`, `npm run check`, `cargo test --manifest-path src-tauri/Cargo.toml tool_installer -- --nocapture`, PowerShell-form `RUSTFLAGS=-D warnings cargo test --manifest-path src-tauri/Cargo.toml tool_installer -- --nocapture`, and `git diff --check` (only LF/CRLF warnings, no whitespace errors).
- Started Phase 16 after the user asked why Claude Desktop installed/displayed `1.14271.0`. Confirmed the official MSIX endpoint is current (`1.15200.0`), but the local machine still had a stale native EXE uninstall registry entry (`AnthropicClaude`, `1.14271.0`, missing install root), and `detector.rs` still returned a winget update command for Claude Desktop on Windows.
- Added red/green tests for the Phase 16 root cause: JS source assertions for stale EXE uninstall cleanup and non-winget detector update routing, plus a Rust detector test requiring the Claude Desktop Windows update command to use the official MSIX path. Verified both failed before implementation.
- Implemented Phase 16 cleanup: Windows Claude Desktop MSIX install/update now runs a pre-download PowerShell cleanup that removes stale deprecated EXE uninstall entries while preserving live EXE installs, and `detector.rs` no longer maps `claude-desktop` into winget update checks or returns `winget upgrade --id Anthropic.Claude`.
- Targeted verification after Phase 16 implementation passed: `npm run test:unit -- src/lib/desktopClientPages.test.mjs`, `cargo test --manifest-path src-tauri/Cargo.toml claude_desktop_windows_update_command_uses_official_msix -- --nocapture`, `cargo test --manifest-path src-tauri/Cargo.toml claude_desktop_windows_msix_install_cleans_stale_exe_registry_entries_first -- --nocapture`, `cargo test --manifest-path src-tauri/Cargo.toml tool_installer -- --nocapture`, and `cargo test --manifest-path src-tauri/Cargo.toml detector -- --nocapture`.
- Final Phase 16 verification passed after formatting the new cleanup helper: `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`, PowerShell-form `RUSTFLAGS=-D warnings cargo test --manifest-path src-tauri/Cargo.toml tool_installer -- --nocapture`, PowerShell-form `RUSTFLAGS=-D warnings cargo test --manifest-path src-tauri/Cargo.toml detector -- --nocapture`, `npm run check`, and `git diff --check` (only LF/CRLF warnings, no whitespace errors).
- Applied the same stale-entry cleanup to the current machine for the previously observed HKCU `AnthropicClaude` uninstall key. The key pointed at missing `C:\Users\dreamyloong\AppData\Local\AnthropicClaude`, was removed, and a registry scan then reported no remaining Claude/Anthropic uninstall entries.
- Started Phase 17 for Windows Main Process Debugger UI automation. Reproduced that the existing PowerShell automation fails when running Claude processes have no `MainWindowHandle`, and still does not open port 9233 after manual MSIX activation creates a visible `Chrome_WidgetWin_1` window. Live screenshots/UIA inspection showed Windows Claude uses an in-window hamburger menu, not a macOS-style top menu, and `Alt`/`F10`/`Alt+F`/`Alt+D` attempts did not open the debugger.
- Found the successful Windows route: after setting the PowerShell process DPI-aware, clicking the Claude window's in-window hamburger menu exposes a UIA `Developer` menu item; its submenu exposes `Enable Main Process Debugger` as a `MenuItemView` `ControlType.CheckBox` with `TogglePattern`. Toggling it opened the official Node inspector on `127.0.0.1:9229`, while `9233` remained closed.
- Added a red/green regression test requiring Windows debugger automation to use UIAutomation, DPI-aware window coordinates, MSIX `shell:AppsFolder` activation, and `Developer > Enable Main Process Debugger`; the test fails on the old `SendKeys('%d')` top-menu script and passes after replacing the script.
- Replaced `request_windows_claude_main_process_debugger_once()` with the verified Windows UIA flow: activate the official MSIX app identity when available, enumerate/restores the `Chrome_WidgetWin_1` Claude window without relying on `MainWindowHandle`, open the in-window hamburger menu with DPI-aware coordinates, click `Developer`, toggle `Enable Main Process Debugger`, and accept any localized confirmation button.
- Live verification passed: after manually turning the debugger off, running the embedded PowerShell script extracted from `src-tauri/src/core/claude_desktop_patch.rs` reopened the official Node inspector at `http://127.0.0.1:9229/json`; `9233` remained closed as expected for this build.
- Final Phase 17 verification passed: `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`, `cargo test --manifest-path src-tauri/Cargo.toml claude_desktop_patch::tests -- --nocapture`, PowerShell-form `RUSTFLAGS=-D warnings cargo test --manifest-path src-tauri/Cargo.toml claude_desktop_patch::tests -- --nocapture`, `npm run test:unit -- src/lib/desktopClientPages.test.mjs`, and `git diff --check` (only LF/CRLF warnings, no whitespace errors).
- Started Phase 18 after the user added a Claude login state and suspected menu/script positions may have shifted. Live UIA probing showed the logged-in window title as `New chat - Claude`; authenticated chat controls appeared in the tree, but the menu route still exposed `Menu`, `Developer`, and `Enable Main Process Debugger`.
- Added a regression assertion requiring the Windows debugger automation to discover `Find-ClaudeMenuButton`, invoke UIA elements through `Invoke-Element`, and keep the old top-left coordinate menu click only after the discovered menu-button path. The test failed before the hardening and passed after implementation.
- Hardened `request_windows_claude_main_process_debugger_once()` so the script prefers UIA InvokePattern/TogglePattern for the hamburger menu, Developer item, debugger toggle, and localized confirmation buttons, with center-click or coordinate fallback only when patterns are unavailable.
- Live Phase 18 verification passed: after turning the debugger off, running the embedded PowerShell script extracted from `src-tauri/src/core/claude_desktop_patch.rs` under the logged-in Claude UI reopened `http://127.0.0.1:9229/json` with target `electron/js2c/browser_init` and `webSocketDebuggerUrl` under `ws://127.0.0.1:9229/...`.
- Final Phase 18 verification passed: `cargo fmt --manifest-path src-tauri\Cargo.toml -- --check`, `cargo test --manifest-path src-tauri\Cargo.toml claude_desktop_patch::tests -- --nocapture`, PowerShell-form `RUSTFLAGS=-D warnings cargo test --manifest-path src-tauri\Cargo.toml claude_desktop_patch::tests -- --nocapture`, `npm run test:unit -- src/lib/desktopClientPages.test.mjs`, and `git diff --check` (only LF/CRLF warnings, no whitespace errors).
- Started Phase 19 to add a language-independent fallback for Windows Claude debugger automation. Current code still has localized-name matching for `Menu`, `Developer`, and `Enable Main Process Debugger`; the missing piece is a structural fallback for unknown languages that can search by menu item shape and TogglePattern instead of UIA Name text.
- Added a red regression assertion requiring `Find-ClaudeMenuItems`, `Find-ClaudeDeveloperMenuByStructure`, and `Find-ClaudeDebuggerToggleByStructure`; it failed on the current name-only fallback as expected.
- Live UIA probing of the opened Claude menu showed the top-level menu as a right-side column of `MenuItemView` entries with `ExpandCollapsePattern` (`File`, `Edit`, `View`, `Developer`, `Help` in English), and the Developer submenu exposes `Enable Main Process Debugger` as the first submenu `MenuItemView` checkbox with `TogglePattern`.
- Implemented the structural fallback: when localized name lookup fails, the script picks the fourth expandable item from the rightmost top-level menu column as Developer, then picks the first wide `MenuItemView` checkbox with `TogglePattern` in the expanded submenu as the debugger toggle. The previous localized name path still runs first.
- Live Phase 19 verification passed after first toggling the debugger off: the embedded script extracted from `src-tauri/src/core/claude_desktop_patch.rs` reopened `http://127.0.0.1:9229/json` with target `electron/js2c/browser_init`.
- Final Phase 19 verification passed: `cargo fmt --manifest-path src-tauri\Cargo.toml -- --check`, `cargo test --manifest-path src-tauri\Cargo.toml claude_desktop_patch::tests -- --nocapture`, PowerShell-form `RUSTFLAGS=-D warnings cargo test --manifest-path src-tauri\Cargo.toml claude_desktop_patch::tests -- --nocapture`, `npm run test:unit -- src/lib/desktopClientPages.test.mjs`, and `git diff --check` (only LF/CRLF warnings, no whitespace errors).
- Started Phase 20 after the user clarified that the script should close the extra window that appears when Main Process Debugger is opened. Live window enumeration after debugger activation showed the main Claude window plus an extra same-process `Chrome_WidgetWin_1` window with no title and an `802x602` client rect, which matches the Inspector/Debugger popup shape rather than the main chat window.
- Added a regression assertion requiring the Windows debugger automation script to contain `Close-ClaudeInspectorPromptWindows`, UIA `WindowPattern.Close()`, `PostMessage`, and `WM_CLOSE`, and to run the close step after the debugger confirmation loop. The test failed before the close-window implementation and passed afterward.
- Live testing showed `PostMessage(WM_CLOSE)` alone leaves the Inspector/Debugger popup visible; UIA `WindowPattern.Close()` hides it while leaving the Node inspector endpoint on `127.0.0.1:9229` alive.
- Hardened the Windows automation further after live runs exposed two flaky states: `Invoke-Element` now catches UIA Invoke/Expand/Toggle COM failures and falls back to center click, and the script clicks the main window before opening the hamburger menu so stale menu state does not hide the Developer submenu.
- Live Phase 20 verification passed: running the embedded script from `src-tauri/src/core/claude_desktop_patch.rs` left `http://127.0.0.1:9229/json` available and window enumeration reported only the main visible Claude `Chrome_WidgetWin_1`; the extra no-title `802x602` prompt window remained non-visible with `VISIBLE_PROMPT_COUNT=0`.
- Final Phase 20 verification passed: `cargo fmt --manifest-path src-tauri\Cargo.toml -- --check`, `cargo test --manifest-path src-tauri\Cargo.toml claude_desktop_patch::tests -- --nocapture`, PowerShell-form `RUSTFLAGS=-D warnings cargo test --manifest-path src-tauri\Cargo.toml claude_desktop_patch::tests -- --nocapture`, `npm run test:unit -- src/lib/desktopClientPages.test.mjs`, and `git diff --check` (only LF/CRLF warnings, no whitespace errors). The frontend test command printed an unrelated oh-my-posh init-file sharing warning before reporting 56 passing tests.
- Started Phase 21 after the user reported that the Windows menu was not localized. Root cause: `build_main_process_injection_source_for_paths()` only installed `installMacosMenuLocalization()` and returned immediately unless `process.platform !== "darwin"` was false, so Windows' in-window Electron app menu never passed through the locale relabeling logic.
- Added a red regression test, `node_inspector_injection_localizes_windows_in_window_menu`, requiring the main-process injection to include a Windows menu popup localization path, `Menu.buildFromTemplate`, `Menu.setApplicationMenu`, `Menu.prototype.popup`, and hardcoded Chinese fallbacks for the Windows top-level app menu. The test failed first on the missing `windowsMenuPopupLocalization` marker.
- Implemented the Windows menu localization hook in `src-tauri/src/core/claude_desktop_patch.rs`: bumped `CSL_INJECTION_VERSION` to `9`, added Windows menu hardcoded labels for top-level and Developer entries, installed `Menu.buildFromTemplate`, `Menu.setApplicationMenu`, and `Menu.prototype.popup` hooks, and kept relabeling reversible by preserving original labels on menu items.
- Live Phase 21 verification used the current Claude Node inspector at `http://127.0.0.1:9229/json`. Injecting the generated runtime into the real Claude main process reported `{ version: 9, popupHook: true, buildHook: true, setHook: true, platform: "win32" }`; after writing the localized launch marker and refreshing, a real `Menu.buildFromTemplate` call returned `文件 | 编辑 | 视图 | 开发者 | 帮助` and Developer submenu labels `启用主进程调试器 | 显示开发者工具 | 打开应用配置文件...`.
- Continued Phase 22/23 after the user rejected coordinate fallback as unsafe when they are operating other software. Added a red regression assertion that the Windows debugger automation must not contain `SetCursorPos`, `mouse_event`, `Click-Point`, `Click-ElementCenter`, `System.Windows.Forms`, `SendKeys`, element `BoundingRectangle`, or `$window.Left/Top/Right/Bottom` coordinate logic.
- Removed the Windows debugger automation's coordinate path entirely. The embedded PowerShell now scopes searches with `AutomationElement::FromHandle($window.Hwnd)`, opens the Claude in-window menu with UIA `InvokePattern`, expands Developer through UIA patterns, toggles Main Process Debugger only through `TogglePattern`, and closes inspector prompt windows through `WindowPattern.Close()` plus `WM_CLOSE` fallback.
- Live verification of the updated embedded PowerShell script exited with code 0 on the current Claude window, and `http://127.0.0.1:9229/json` returned the expected Node inspector target `electron/js2c/browser_init`. Verification passed: `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`, `cargo test --manifest-path src-tauri/Cargo.toml windows_debugger_automation_uses_in_window_menu_not_alt_top_menu -- --nocapture`, `cargo test --manifest-path src-tauri/Cargo.toml claude_desktop_patch::tests -- --nocapture`, PowerShell-form `RUSTFLAGS=-D warnings cargo test --manifest-path src-tauri/Cargo.toml claude_desktop_patch::tests -- --nocapture`, `git diff --check`, and a repository scan excluding `src-tauri/target` found no old coordinate/mouse automation symbols.
- Investigated why a repackaged build did not trigger the new Claude runtime logic. The on-disk `translation-runtime.js` cache still updates by content through `ensure_patch_files()`, but the already-injected Claude main-process object reused itself whenever `CSL_INJECTION_VERSION` stayed at `9`, so changes to the generated injection wrapper or runtime payload could be skipped until a manual version bump or process restart.
- Added a red/green regression test, `node_inspector_injection_reinstalls_when_injection_changes_without_version_bump`, and fixed the injection freshness check by embedding a SHA-256 `CSL_INJECTION_SIGNATURE` derived from `claude_desktop_patch.rs`, `TRANSLATION_RUNTIME`, and bundled Claude locale payloads. Reuse now requires both version and signature to match; mismatches call the old object's `dispose()` and reinstall the injection, including per-webContents signature state.
- Verification for Phase 24 passed: `cargo test --manifest-path src-tauri/Cargo.toml node_inspector_injection_reinstalls_when_injection_changes_without_version_bump -- --nocapture`, `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`, `cargo test --manifest-path src-tauri/Cargo.toml claude_desktop_patch::tests -- --nocapture`, PowerShell-form `RUSTFLAGS=-D warnings cargo test --manifest-path src-tauri/Cargo.toml claude_desktop_patch::tests -- --nocapture`, `cargo test --manifest-path src-tauri/Cargo.toml extract_runtime_injection_to_temp_when_requested -- --nocapture`, and `git diff --check` (only LF/CRLF warnings).
- Started Phase 25 after the user reported the auto-activation issue still occurs. Current live state: Claude Desktop MSIX `1.15200.0.0` is running, manual injection can work when the debugger is opened, but `http://127.0.0.1:9229/json` is not currently available and the embedded Windows UIA script previously failed with `Claude Developer menu could not be opened through UI Automation.`
- Reproduced Phase 25 with a diagnostic copy of the embedded PowerShell script. `Open-ClaudeMenu` returned true before the hamburger menu opened because `Find-ClaudeMenuElement` selected a main-window `Developer` `SubmenuButton` instead of a popup menu item; `Invoke-Element` then returned false and produced `Claude Developer menu could not be opened through UI Automation.`
- Fixed Phase 25 by adding popup-only Developer/debugger selector functions, making `Open-ClaudeMenu` accept only non-main same-process popup menu roots, and adding `%USERPROFILE%\.codestudio-lite\claude-desktop-patch\windows-main-debugger.log` diagnostics with selected element details.
- Live Phase 25 verification passed: before the fixed script, `http://127.0.0.1:9229/json` timed out; after running the embedded script, it returned the `electron/js2c/browser_init` Node inspector target on `127.0.0.1:9229`. Verification commands passed: `cargo test --manifest-path src-tauri/Cargo.toml windows_debugger_automation_does_not_treat_main_window_submenu_as_open_menu -- --nocapture`, `cargo test --manifest-path src-tauri/Cargo.toml claude_desktop_patch::tests -- --nocapture`, `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`, and PowerShell-form `RUSTFLAGS=-D warnings cargo test --manifest-path src-tauri/Cargo.toml claude_desktop_patch::tests -- --nocapture`.
- Started Phase 26 after the user reported that Claude sometimes opens a blocking ad/promo overlay whose close control is an unnamed X. Current live UIA inspection did not show the main chat window or the promo overlay, and extracting/searching the installed `app.asar` did not reveal a clear local promo implementation, suggesting the overlay is remote page content.
- Implemented Phase 26 in `src-tauri/src/core/claude_desktop_patch.rs`: added Windows-only `Close-ClaudeBlockingOverlayWindows`, `Find-ClaudeAnonymousOverlayCloseButton`, and `Test-ClaudeOverlayCandidateText`, then call the cleanup before `Open-ClaudeMenu`. The cleanup remains Claude-window scoped and UIA-only; it does not use coordinates or macOS logic.
- Verification for Phase 26 passed: `cargo test --manifest-path src-tauri/Cargo.toml windows_debugger_automation_closes_anonymous_blocking_overlay_before_menu -- --nocapture`, `cargo test --manifest-path src-tauri/Cargo.toml claude_desktop_patch::tests -- --nocapture`, PowerShell-form `RUSTFLAGS=-D warnings cargo test --manifest-path src-tauri/Cargo.toml claude_desktop_patch::tests -- --nocapture`, `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`, and `git diff --check -- src-tauri/src/core/claude_desktop_patch.rs task_plan.md findings.md progress.md` with only the existing LF/CRLF warning.
- Started Phase 27 after the user reported that Windows DevTools language did not follow the language option and the Claude Tray menu was not localized. Added regression coverage for DevTools title resync after language changes and for Windows Tray context-menu localization.
- Implemented Phase 27 in `src-tauri/src/core/claude_desktop_patch.rs`: DevTools titles now sync for both `devtools://` and `chrome-devtools://` windows on language refresh/title/load/devtools-opened events, and Windows Tray menus are localized through `Tray.prototype.setContextMenu` with known-menu retranslation on locale changes. The Windows menu and Tray hooks share reversible original-label tracking without changing install, uninstall, or debugger activation logic.
- Verification for Phase 27 passed: `cargo test --manifest-path src-tauri/Cargo.toml node_inspector_injection_syncs_windows_devtools_title_after_language_changes -- --nocapture`, `cargo test --manifest-path src-tauri/Cargo.toml node_inspector_injection_localizes_windows_tray_menu -- --nocapture`, `cargo test --manifest-path src-tauri/Cargo.toml claude_desktop_patch::tests -- --nocapture`, `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`, PowerShell-form `RUSTFLAGS=-D warnings cargo test --manifest-path src-tauri/Cargo.toml claude_desktop_patch::tests -- --nocapture`, and `git diff --check -- src-tauri/src/core/claude_desktop_patch.rs task_plan.md findings.md progress.md` with only the existing LF/CRLF warning.
- Started Phase 28 after the user reported awkward Claude zh-CN strings "通过繁琐的任务坚持" and "船只特征，而非线条". Search showed both are static Ion locale entries in `src-tauri/resources/claude-desktop/i18n/ion-dist/i18n/zh-CN.json`, not runtime fallback output.
- Added the failing regression test `bundled_zh_locale_avoids_literal_task_and_shipping_translations`; it initially failed because `4ahpF5N/t0` still contained "通过繁琐的任务坚持". Fixed `4ahpF5N/t0` to "推进繁琐任务" and `ye9sGm7rX3` to "发布功能，而不是堆代码行数".
- Verification for Phase 28 passed: `cargo test --manifest-path src-tauri/Cargo.toml bundled_zh_locale_avoids_literal_task_and_shipping_translations -- --nocapture`, `cargo test --manifest-path src-tauri/Cargo.toml bundled_zh_locale_payloads_are_valid_and_substantial -- --nocapture`, `cargo test --manifest-path src-tauri/Cargo.toml claude_desktop_patch::tests -- --nocapture`, `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`, PowerShell-form `RUSTFLAGS=-D warnings cargo test --manifest-path src-tauri/Cargo.toml claude_desktop_patch::tests -- --nocapture`, and `git diff --check -- src-tauri/src/core/claude_desktop_patch.rs src-tauri/resources/claude-desktop/i18n/ion-dist/i18n/zh-CN.json task_plan.md findings.md progress.md` with only the existing LF/CRLF warning.
- Started Phase 29 after the user reported that Windows still did not reliably close the Inspector popup, and that delayed debugger automation could be pushed back by user mouse/focus activity until localization injection gave up. Inspection showed the script always attempted AppX activation before checking for an already-open Claude window, and Inspector cleanup was a single scan rather than a short wait around the actual toggle/prompt timing.
- Added red/green tests `windows_debugger_automation_prefers_existing_window_before_appx_activation` and `windows_debugger_automation_polls_to_close_inspector_prompt`. The first failed because `Start-ClaudeWindowsApp` ran before the first `Get-ClaudeMainWindow`; the second failed because no `Wait-CloseClaudeInspectorPromptWindows` helper existed.
- Implemented Phase 29 in `src-tauri/src/core/claude_desktop_patch.rs`: the UIA script now uses an existing Claude window before AppX activation, trims focus/menu/developer waits from 400/350/700 ms style sleeps to short 100/120 ms waits, and polls `Close-ClaudeInspectorPromptWindows` around debugger toggle and confirmation handling. The script remains UIA-only and a scan confirmed no coordinate, mouse, SendKeys, or old long-wait symbols reappeared.
- Verification for Phase 29 passed: `cargo test --manifest-path src-tauri/Cargo.toml windows_debugger_automation_prefers_existing_window_before_appx_activation -- --nocapture`, `cargo test --manifest-path src-tauri/Cargo.toml windows_debugger_automation_polls_to_close_inspector_prompt -- --nocapture`, `cargo test --manifest-path src-tauri/Cargo.toml claude_desktop_patch::tests -- --nocapture`, `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`, PowerShell-form `RUSTFLAGS=-D warnings cargo test --manifest-path src-tauri/Cargo.toml claude_desktop_patch::tests -- --nocapture`, `git diff --check -- src-tauri/src/core/claude_desktop_patch.rs task_plan.md findings.md progress.md`, and a source scan for old coordinate/mouse/long-wait symbols.
- Started Phase 30 after the user reported that changing pages or controls during a refresh could be overwritten by the late refresh result. Inspection showed Codex Client route-entry refreshes intentionally render cached data while a background scan runs, but `applyState()` still replaced `settingsDraft` on completion; Settings `loadSettings()` similarly overwrote controls after async load.
- Added red/green regression coverage in `src/lib/codexClientLaunch.test.mjs` for preserving Codex Client draft edits while a scan is in flight, and in `src/lib/settingsLanguage.test.mjs` for ignoring stale Settings initial loads after local edits. Both tests failed before the guards and passed after implementation.
- Implemented Phase 30 in `src/lib/codexClientStore.ts` and `src/routes/Settings.svelte`: Codex Client tracks the refresh-start `settingsSaveRevision` and calls `applyState(..., { preserveDraft })` whenever a refresh result is older than the current draft; Settings increments `settingsEditRevision` on each user edit and refuses to apply stale load results to visible controls.
- Verification for Phase 30 passed: `npm run test:unit -- src/lib/codexClientLaunch.test.mjs`, `npm run test:unit -- src/lib/settingsLanguage.test.mjs`, combined `npm run test:unit -- src/lib/codexClientLaunch.test.mjs src/lib/settingsLanguage.test.mjs`, and `git diff --check -- src/lib/codexClientStore.ts src/lib/codexClientLaunch.test.mjs src/routes/Settings.svelte src/lib/settingsLanguage.test.mjs task_plan.md findings.md progress.md` with only expected LF/CRLF warnings.
